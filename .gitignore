# Environment - NEVER commit
.env

# SSH keys - NEVER commit
config/openclaw-docker-key
config/openclaw-docker-key.pub

# Logs
logs/*.log
logs/squid/

# Rate limit files (runtime)
*.rate-limit
*.rate-limit.lock
*.rate-limit.tmp.*
