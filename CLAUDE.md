# Zupee Claw - Claude Code Project Guide

## Architecture

Zupee Claw is a secure, air-gapped AI development partner. It runs inside Docker with local LLM inference via Ollama and delegates coding tasks to Claude Code through a locked-down SSH gateway.

**Key separation:**
- `zupee-claw/` = Claw's home (configs, agent data, sessions, memory, scripts, Docker)
- `$WORKSPACE_DIR/` = actual dev codebase (where Claude Code operates)

**Data flow:**
```
Browser -> localhost:3000 (Claw Web UI)
              |
              v
      Claw Gateway (Docker) --> Ollama LLM (Docker)
              |
              | SSH (ForceCommand, whitelisted only)
              v
      Host: ssh-gateway.sh -> allowed-commands.conf
              |
              +-- service-status.sh, git-status.sh, git-pull.sh
              +-- run-tests.sh, run-claude.sh
                      |
                      v
                  Claude Code (locked to $WORKSPACE_DIR/$REPO)
```

## Directory Map

```
zupee-claw/
├── .env.example              # Environment config template
├── setup.sh                  # End-to-end provisioning
├── cleanup.sh                # Full teardown
├── CLAUDE.md                 # This file
├── bin/                      # Whitelisted scripts (mounted :ro into Docker)
│   ├── allowed-commands.conf # Allowlist of permitted commands
│   ├── ssh-gateway.sh        # SSH ForceCommand - validates & routes
│   ├── workspace-env.sh      # Shared workspace resolver (sourced by others)
│   ├── run-claude.sh         # Launch Claude Code (locked down)
│   ├── git-status.sh         # Git status on repo
│   ├── git-pull.sh           # Git pull with rebase
│   ├── run-tests.sh          # Run tests in repo
│   └── service-status.sh     # Check service health
├── config/
│   ├── claude-settings.json  # Claude Code lockdown (allow/deny tool lists)
│   ├── sshd_openclaw.conf    # Host SSH hardening template
│   ├── openclaw-docker-key   # SSH private key (generated by setup.sh)
│   └── authorized_keys       # ForceCommand-restricted key
├── openclaw-home/             # Maps to ~/.openclaw inside container
│   ├── openclaw.json          # Main Claw config
│   └── workspace/             # Agent personality & memory
│       ├── AGENTS.md          # Operating instructions
│       ├── SOUL.md            # Persona and tone
│       ├── TOOLS.md           # Available tools reference
│       └── memory/            # Runtime memory (created by agent)
├── docker/
│   ├── Dockerfile             # Claw + SSH client
│   ├── docker-compose.yml     # Full stack (openclaw + ollama)
│   └── entrypoint.sh          # Container startup
├── docs/
│   └── architecture.md        # Full architecture documentation
└── logs/                      # Audit logs (gitignored)
```

## Security Layers (11 layers)

| # | Layer | What | Blocks |
|---|-------|------|--------|
| 1 | Docker | Process/filesystem isolation | Direct host access |
| 2 | 127.0.0.1 bind | Web UI on localhost only | Network exposure |
| 3 | SSH ForceCommand | Single entry point to host | Arbitrary command execution |
| 4 | ssh-gateway.sh | Command allowlist + logging | Unapproved scripts |
| 5 | Script arg validation | Input sanitization (no metacharacters, no `..`) | Injection attacks |
| 6 | Claude --permission-mode dontAsk | Tool whitelist mode | Unapproved tools |
| 7 | Claude --disallowedTools | Tool blacklist (ssh, curl, rm -rf, sudo, docker) | Dangerous tools |
| 8 | Claude --max-turns 25 | Execution limit | Runaway loops |
| 9 | Claude --max-budget-usd 10 | Cost cap | API cost overruns |
| 10 | rbash user | Restricted shell for openclaw-bot | Shell escapes |
| 11 | Read-only mount | bin/ mounted :ro in Docker | Script tampering |

## Network Architecture

Two Docker networks:
- **`isolated`** — `internal: true`, bridge. Ollama + Claw communicate here. No outbound internet.
- **`host-access`** — `internal: true`, bridge. Claw uses this for SSH to host via `host.docker.internal`.

Port binding: `127.0.0.1:3000:3000` (Web UI, localhost only).

## Host Commands (via SSH Gateway)

| Command | Script | Purpose |
|---------|--------|---------|
| `git-status [repo]` | `bin/git-status.sh` | Check working tree |
| `git-pull [repo]` | `bin/git-pull.sh` | Pull latest with rebase |
| `run-tests [repo]` | `bin/run-tests.sh` | Run test suite |
| `run-claude <prompt> [repo]` | `bin/run-claude.sh` | Coding tasks (25 turns, $10 cap) |
| `service-status` | `bin/service-status.sh` | Host health + list repos |

Default repo comes from `REPO` in `.env`. All commands accept optional `[repo]` override.

To add a new command: create `bin/<name>.sh`, add `<name>` to `bin/allowed-commands.conf`.

## Claude Code Lockdown

Managed via `config/claude-settings.json` and `bin/run-claude.sh` flags.

**Allowed tools:** Read, Edit, Write, Glob, Grep, npm (test/run/install/ci), git (read ops + add/commit/branch/checkout/stash), node (specific files), ls, mkdir, rm (single files by ext), wc, head, tail, sort, which.

**Blocked tools:** node -e/npx, curl/wget/ssh, sudo/docker, python/ruby/perl/awk/sed, rm -rf, bash -c/sh -c/eval/exec, git push/rebase/reset/merge/config, WebFetch, WebSearch.

**Limits:** 25 turns max, $10 budget per run.

## Docker Services

| Service | Container | Image | Resources | Networks |
|---------|-----------|-------|-----------|----------|
| `ollama` | `zupee-ollama` | `ollama/ollama:0.6.2` | 8G RAM, 4 CPU | `isolated` |
| `openclaw` | `zupee-claw` | Built from `docker/Dockerfile` | 2G RAM, 2 CPU | `isolated`, `host-access` |

Volume mounts:
- `openclaw-home/` personality files → `:ro` (agent can't rewrite its own rules)
- `openclaw-home/` memory/sessions → `:rw` (agent runtime data)
- `bin/` → `:ro` (scripts can't be tampered with from container)
- `config/openclaw-docker-key` → `:ro` (SSH key)
- `logs/` → `:rw` (audit trail)

## Shell Script Conventions

All scripts in `bin/`, `setup.sh`, `cleanup.sh`, `docker/entrypoint.sh` follow:
- `set -euo pipefail` at top
- Source `workspace-env.sh` for workspace resolution (in bin/ scripts)
- Parse `.env` safely with `grep`/`cut` — never `source .env`
- Validate inputs: reject shell metacharacters (`; | & $ \` \ ( ) { } < >`), path traversal (`..`), absolute paths
- Log actions to `~/openclaw/logs/` with ISO timestamps
- Use colored output helpers: `info()`, `warn()`, `error()`, `step()`

## Commit Conventions

- Conventional commits: `feat:`, `fix:`, `docs:`, `chore:`
- One concern per PR
- Feature branch from `main` — never push to main directly
- ShellCheck must pass on all `.sh` files
- Test full setup/cleanup cycle if changing `setup.sh` or `cleanup.sh`
