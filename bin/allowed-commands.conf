# Allowed commands for SSH gateway
# Each line is a command name (without .sh extension).
# The gateway resolves <name> to bin/<name>.sh and executes it.
git-status
git-pull
run-tests
run-claude
service-status
# Add your own commands here (create bin/<name>.sh to match)
